{
  "name": "render-network",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ InstaClean/render-network",
    "create-local": "graph create --node http://localhost:8020/ InstaClean/render-network",
    "remove-local": "graph remove --node http://localhost:8020/ InstaClean/render-network",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 InstaClean/render-network"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
